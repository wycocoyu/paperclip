/**
 * 文档骨架（MUL-467）：`document:get` 读一个还不存在的键时，返回模板而不是
 * 只报 404。落点选 get 不选 put，是因为追加流程本来就必须先 get 读回全文
 * （put 是覆盖式，不读会丢数据），所以新建那一次也会先 get —— 骨架在这里
 * 出现，写入方不需要记得任何额外动作。put 侧预填则要求先想起来发一次空
 * put，那仍然靠记忆，正是要治的东西。
 *
 * 只做 CLI 侧：服务端 GET 路由保持 404 语义，别的消费方（UI、issue
 * discussion 归档的 answerDocKey 读取）靠它判存在性，不动。
 *
 * 正文住在 shared 而不是 CLI（Team Templates 页面）：写模板的是 CLI，读模板
 * 长什么样的是人，两边必须看同一份字符串，否则页面上展示的和 `document:get`
 * 吐出来的会各自漂移。
 */
export const DOCUMENT_SKELETONS: Record<string, string> = {
  glossary: `# 本卡术语 · <卡号>

> 收词两门都要满足：① 有一条已定决策规定了这个词在本卡的含义 ② 按通用含义理解会改变本卡的范围、行为、验收或责任人。只是换个说法、表达偏好的不收。
> 定义一两句封顶，说它**是什么**不是它做什么。同一概念有多个说法时挑一个，其余进 \`_别用_\`。
> 讨论中当场写，别攒到收卡前；收尾时对一遍 decision-log 查漏。

## 词条

**<词>**（说人话：<一句通俗版，黑话必带>）
<一两句话说它是什么>。#<定它的那条 decision-log 编号>
_别用_：<同义的其他说法，逗号分隔>

## 待对齐

<讨论中卡住过但还没定的词义疑惑。没有就删掉这一节。>
`,
  "decision-log": `# decision-log · <卡号>

> 用 v2 模板记录。旧条目不删，就地在状态行标注被谁覆盖。推翻旧决策时，改旧条目的状态行和追加新条目必须放进同一次 document:put，分两次做很容易只做后一半。漏改状态行，decisions:pull 就会把已经作废的条目也算成已定。
>
> 这张卡如果造了新词（别处没有，或含义跟别处不同），另开一份 \`glossary\` 文档记它，别写在这里。同一件事本来就有通俗说法、只是被压缩成一个词的，那是黑话，换成人话不要进表。没造新词就不用写。
>
> 每条七格，顺序固定：问题 / 老板说 / 我推荐 / 老板采纳 / 对审意见 / 最终答案 / 落点，一条只回答一个问题。老板没直接发话的条目，「老板说」那格写明「本条老板未直接发话，由我主动记录」，格不能空着。
>
> 格的顺序就是这条决策的走法：我给推荐 → 老板当场表态 → 需要的话交对审 → 综合两边写最终答案。所以「老板采纳」记的是老板当时的立场，不一定是终裁，终裁在「最终答案」。老板表态时还不确定、想等对审再说的，标题行状态先写「待定」，对审回来综合完再改「已定」。收卡门禁只认「已定」，悬着的条目不会被当成已经定了。
>
> 「对审」只算另一个终端模型（Codex / Zcode / Cursor / Grok）。子 Agent 是「我」派出去的，它的结论算进「我推荐」。老板的立场走「老板说」和「老板采纳」两格。对审不是每条都要走，要不要送审由老板临时决定，没送审的就在这一格写「未审」。这一格每条都出现，是为了让人一眼看出这条有没有经过第二个模型，「未审」是事实标记不是罪状。
>
> 「对审意见」由写卡的这一方转录，对审方只回意见不改卡，结论句照抄对面原话不要改写成自己的说法（转录的人恰好就是被审的人）。写一句立场，不是再列一遍选项：同意还是异议、针对采纳的第几条、一句结论性理由，带上评审方模型名（每条可能换人）。对面给出没人列过的第三方案也写在这里，本条的「问题」没变，它只是同一个问题的另一个候选答案；评审引出的是新问题，那才另起一条。评审全文不进卡，完整结论发回派单方，progress 只留一行，这一格是对面立场在卡上的唯一留痕，一句话写足，别写成评审报告。
>
> 对审有异议而老板还没看过的，这条保持「待定」，「最终答案」先空着。不许自己把异议综合掉。
>
> 七格是模板要求，程序当前只硬校验其中四格（老板说 / 我推荐 / 老板采纳 / 落点）。「问题」「最终答案」「对审意见」没被程序拦住，但缺了下游提炼不出这条定了什么、也看不出审没审，照样得写。

---

## 1 · YYYY-MM-DD HH:MM · 待定 / 已定 / 已被第 N 条推翻

**问题**：这一条要定的是哪一件事，一句话

**老板说**

> 原话照抄，不转述、不截断

**我推荐**

1.
2.

**老板采纳**：全部 / 只 ① / ①③，其余未表态。这是老板当时的立场，不确定、想等对审的把状态写「待定」 —— 「我推荐」非空时此格必填

**对审意见**：未审 / <评审模型名> 同意 / <评审模型名> 异议（对采纳的第 N 条），一句话结论性理由

**最终答案**：综合老板立场与对审意见之后，这条最后定成什么，一句话说完，不写过程

**落点**：文件 + 章节 + 那一段的加粗导语（不写行号，行号会漂）

**推翻原因**：仅在状态为「已被第 N 条推翻」时出现
`,
};

/** 有骨架的文档键，按页面展示顺序。 */
export const DOCUMENT_SKELETON_KEYS = Object.keys(DOCUMENT_SKELETONS);

export function documentSkeleton(key: string): string | undefined {
  return DOCUMENT_SKELETONS[key];
}
